# SkillNet — 重新设计 v2(中文版)

**状态:** 重新设计提案。取代 `skillnet-project-document-v1.md` 中的架构与现状描述。
**日期:** 2026-06-21
**依据:** 对实际代码库(合约、demo 服务器、HCS-26 集成、前端)做的 4 个维度审计,并与 v1 提案逐项对照。

---

## 1. 执行摘要

v1 文档描述的是一个三层去中心化协议——链上技能 NFT、质押验证者网络、递归版税、TEE 执行、x402 支付、治理 DAO。但**真正在运行的东西,是一个单一的中心化 Node/Express 进程**(`demo/server.js`),所有状态都存在内存里,并用 JavaScript 重新实现了三个 Solidity 合约。唯一**真正去中心化且能用**的部分只有一处:把技能注册信息发布/读取到一个真实的 Hedera HCS-26 topic(`demo/hcs26.js`)。

宣传与实物之间的差距很大,某些地方甚至是事实性错误:

- **合约根本没部署。** `contracts/.env` 里的 `SKILL_NFT_ADDRESS` / `COMPOSITION_DAG_ADDRESS` / `FEE_ROUTER_ADDRESS` 全是空的;前端把它们默认成 `0x000…000`。而 v1 §13 却写着 "Deployed (Base Sepolia)"。这是文档中最关键、也最不真实的一条声明。
- **验证者网络是伪造的。** `/api/validate` 只调用了**一次** Qwen,然后通过加随机扰动捏造出三个"验证者"并取中位数。没有质押、没有罚没、没有节点、没有 oracle 合约。
- **x402 支付与 ERC-8004 身份都是模拟的**(`network: "sim-local"`、一个 `crypto.randomUUID()` 当"收据"、自增整数当"agent id")。
- **递归版税的数学,恰恰在协议存在意义所依赖的那种图上是错的**(见 §5)。种子数据里的旗舰技能 "Full Audit Suite" 会把上游池子超额分配到 **110%**——在 JS 里悄悄少付创作者,在 Solidity 里直接**回滚**(`panic 0x11`)。它没被测出来,因为唯一的版税测试只覆盖了一个最简单的深度 1、两个叶子节点的情况。
- **TEE、IPFS/Arweave、状态通道、策展市场、治理/代币、链上 5 维质量分、HOL Registry Broker / HCS-1 / HCS-10 集成,在代码里都不存在。**

**真正好的部分:** Solidity 合约干净易读(OpenZeppelin ERC-721、pull 模式提现、BFS 环检测);demo 忠实且内部注释诚实;而 **HCS-26 注册表是真实可用的,也是能立刻交付真实价值的最小单元**——无需许可、防篡改、不可下架的技能注册。

**重新设计的核心动作:** 别再去卖一个其实不存在的去中心化协议。把产品锚定在唯一真实的去中心化能力上(HCS-26 注册 + 发现),在**一条**测试网上用**修复后**的版税数学把经济模型做实,先上线一个诚实的单一层级,把所有"假设需求已存在"但需求其实还不存在的东西**推迟**。把优雅的组合-DAG/版税机制作为长期差异化保留——但只有在有真实调用量时,才让价值真正流经它。

---

## 2. 真实 vs 模拟 vs 缺失(审计精炼)

| v1 声明 | 实际情况 | 状态 |
|---|---|---|
| 合约 "Deployed (Base Sepolia)" | `.env` 地址为空;前端指向 `0x0` | **误导** |
| 链上 NFT / DAG / FeeRouter 驱动产品 | `server.js` 在内存里重实现了三者;合约未被使用 | **模拟** |
| NFT 元数据里的 5 维质量分 | `SkillMetadata` 结构体没有 scores/consensus/topic 字段 | **缺失** |
| 去中心化质押验证者网络(§5) | 1 次 LLM 调用 + 随机扰动捏造"3 个验证者" | **模拟** |
| mint 前的预发布验证门槛 | `mintSkill` 是 `external` 且无访问控制;任何人都能 mint 任何东西 | **缺失** |
| x402 机器支付 + ERC-8004 身份 | `sim-local` 网络、UUID 收据、整数 agent id | **模拟** |
| HCS-26 无信任技能注册 | 真实测试网发布 + mirror node 读取——**但只锚定元数据,不锚定代码/内容哈希** | **部分(真实但狭窄)** |
| HCS-1 存储 / HCS-2 版本 / HCS-10 通信 / Registry Broker | 仅 HCS-2 版本 topic 用了一部分;其余缺失 | **基本缺失** |
| TEE(Phala/Oasis)用于 SHIELDED/PRIVATE | 任何地方都没有 TEE;层级只是装饰性字符串 | **缺失** |
| IPFS/Arweave 内容 + 5 层 CDN | `ipfs://QmPlaceholder…` 占位符;无客户端 | **缺失** |
| 状态通道 / 预付押金 / 双模支付 | `payForCall` 只有单次按调用付费 | **缺失** |
| 治理 DAO + 代币、代理升级、多签 | 都不存在;参数是硬编码字面量 | **缺失** |
| 策展市场 / 联合曲线 | 只是 UI 里一个 2% 的数字;没有可质押的东西 | **缺失** |

**今天真实的信任模型:** *信任运营方那一台服务器*,外加一份真实但狭窄的、记录自我声明元数据的 Hedera 日志。不是"无需许可、可验证、主权自持"。

---

## 3. 推荐方向(以及战略岔路)

审计考量了三个北极星。它们并不互斥——建议把 **A 作为主干**,并把 **B** 与 **C** 的精华嫁接上去。

**▶ 方向 A — 务实的、信任锚定的"注册表优先"(推荐)。**
诚实承认:这是一个高性能服务,它唯一的去中心化资产是 HCS-26 注册表。先卖*可验证、不可下架的技能注册 + 发现*。执行与结算先放在链下,保持 web2 速度。以周为单位上线,而非季度。

**方向 B — 真正的链上协议。** 认真做加密原生愿景,但要做*对*:修好版税数学,真正部署到**一条**链,把身份/支付做成真的。最重的路径。**从 B 嫁接:**把(本就不错的)合约真正部署到一条测试网并修好数学——这能把经济模型从纸面变成可度量的东西。

**方向 C — Agent 经济 / MCP 原生。** 围绕"agent 在运行时通过 MCP + x402 发现并付费调用技能"重构。这是押注一个还不存在的市场。**从 C 嫁接:**保留 MCP server 与 agent demo 作为证明"机器可用"的*接口*——它已经做好了,而且是个可信的差异点。

**为什么以 A 为主干:** 每个审计维度都收敛到同一结论——**今天没有用户**、合约没部署、"去中心化"那几层是模拟的,而唯一能用的东西(HCS-26)恰恰也是唯一有可防御、可立即上线价值主张的东西。B 和 C 都预设了项目尚未证明的需求。A 现在就交付真实价值,同时为通往 B/C 保留可信路径。

> **留给负责人的决策(§11):** 确认"A 为主干",或明确选择 B 或 C 作为主押注。这一个选择会重塑整个建设计划。

---

## 4. 重新设计的架构

三个诚实的层。划分依据是:*什么是真正需要无信任的* vs *什么只是需要快速且正确*。

```
第 3 层 — 接口  (链下,快)
  MCP server(agent 可用)+ REST + 轻量 Web UI
  在注册表上做发现/搜索;调用路由;执行

第 2 层 — 经济  (在一条测试网上,已部署 + 已修复)
  SkillNFT(所有权 + 价格 + 版本 + HCS topic id + 质量分哈希)
  CompositionDAG(带 visited-set 的环/深度/权重校验)
  结算(链下凭证 → 周期性链上批量分账)

第 1 层 — 信任锚  (真正去中心化)
  Hedera 上的 HCS-26 发现注册表  ← 真正的护城河
  每条注册携带一个 SHA-256 内容/manifest 哈希
  公开 mirror node = 任何人可验证、任何人不可下架
```

**链上 vs 链下的诚实划分:**

- **链上(便宜、罕见、只做验证):** 所有权、价格、版本血缘、技能 manifest 哈希与质量报告哈希,以及**批量**结算。链只在 mint、争议、每个结算批次各触碰一次——**绝不**按每次调用触碰。
- **HCS-26(去中心化、防篡改):** 技能注册与版本历史的公共注册表,现在加入内容哈希,使其锚定*完整性*,而不仅仅是一个名字。
- **链下(快、可变、运营方运行):** 发现/搜索、执行(LLM/工具调用)、按调用计费、质量检查器。这些明确*不是*无信任的,文档要如实说明。

**数据模型修正:**
- 给 `SkillNFT.SkillMetadata` 加上:`holTopicId`(HCS-26 链接)、`manifestHash`(bytes32),以及 5 个分数或一个由授权打分方写入的 `scoresHash`。在它存在之前,不要声称"链上质量"。
- 让 HCS-26 消息包含 `contentHash`,使铭文锚定技能本身,而非仅仅标签。
- 费用数学只能有一个事实来源:部署合约并让 demo *调用它们*(via viem),或者保留 JS 模拟器但加上"逐位对齐 Solidity"的一致性测试。不要再有两份会发散的重实现。

---

## 5. 修正后的经济模型

> **已实现(2026-06-22)。** 下面的守恒 ρ-flow 已落地到 `FeeRouter.sol`(`_royalty`)并在 demo(`server.js`)中同步实现,上线参数 `PROTOCOL_SHARE = 10%`、`RHO = 20%`(§5.3 算例用 ρ=0.3 仅为示例;ρ 是唯一的治理旋钮)。新增 Foundry 测试——`testBundleOfBundlesConservesAndDoesNotRevert`、`testDiamondConserves`、`testFuzz_ConservationDiamond`(256 次随机)——证明在过去会回滚的"组合之组合"形状下 `Σ余额 == 支付额`;全部 11 个测试通过,demo 种子数据也精确守恒到 wei。

### 5.1 这个 bug(精确描述)

`FeeRouter._distributeUpstream`(及其 JS 孪生)给每个**深度带**分配上游池 `U` 的固定比例——`[50, 30, 15, 5]%`——但这个带比例在**每个分支上各重算一次**。每个组合的权重只在*本地*加起来等于 100,而不是跨整个带。于是:

```
总分配 = Σ_depth ( 带比例[depth] × 该深度上的组合节点数 )
```

单一线性链加起来是 `50+30+15+5 = 100%`,守恒。但旗舰功能——*bundle of bundles(束的束)*——在同一深度上有 ≥2 个组合节点:

```
种子数据 "Full Audit Suite" #8 = compose(#6, #7, #5)   其中 #6=(#1,#2), #7=(#3,#4)
  深度 1:  0.50·U          (在 #6,#7,#5 之间分)
  深度 2:  0.30·U (经 #6)  +  0.30·U (经 #7)   = 0.60·U
  合计  =  1.10·U  >  U
```

- **JS demo:** `creatorTotal = creatorBase + (U − 1.10U)` → 创作者悄悄损失 `0.02·V`;宣传的 70/20/10 变成 **68/22/10**。
- **Solidity:** `upstreamTotal − upstreamDistributed` 在 0.8 的检查算术下下溢 → **`panic 0x11` → `payForCall` 回滚**。任何"组合之组合"都**永久无法在链上调用**。

它能逃过 review,只因为 `testRecursiveRoyalties` 用了两个*叶子*父节点(深度 2 的递归从未运行),而链上的 `Seed.s.sol` 恰好用了一个*每个深度只有一个组合节点*的不同图(0.95U),跟 demo(1.10U)不同。两份重实现已经发散,而且都没触发这个失败。

### 5.2 修复方案:带单一衰减参数 ρ 的权重比例流

彻底丢掉深度表。用一条对**任意** DAG(链、菱形、束的束)都可证明守恒的递归规则:

```
支付 V:
  protocol = V · p                      → 国库            (例如 p = 0.10)
  N        = V · (1 − p)                → 从被调用技能进入 DAG
  royalty(被调用技能, N)

royalty(node, amount):
  if node 有父节点(边权 w_i, Σ w_i = 1):
      creator[node] += amount · (1 − ρ)            # 本节点留下 (1−ρ)
      for 每个父节点 i:  royalty(parent_i, amount · ρ · w_i)
  else:                                            # 叶子
      creator[node] += amount                      # 留下全部份额
```

**守恒证明(对子树归纳):** 叶子恰好分出 `amount`。非叶子留下 `amount·(1−ρ)`,把 `amount·ρ` 按和为 1 的权重分出;由归纳假设每个父子树都分出其输入,故该节点子树分出 `amount·(1−ρ) + amount·ρ = amount`。根子树分出 `N`;加上 protocol `V·p`,合计 `= V`。∎

性质:永不下溢(没有余项相减);深度衰减自然以 `ρ^depth` 出现;菱形里被共享的父节点会正确地从每条路径累积(它*确实*贡献给了每个分支);`ρ` 是唯一的治理旋钮(例如 `ρ = 0.25` ≈ "每跳创作者留 75%")。可在一次拓扑遍历中计算,O(V+E)——既约束 gas,也消除逐节点重走。

### 5.3 多分支算例(现在能对平了)

`V = 0.015 ETH`、`p = 0.10`、`ρ = 0.30`,demo 图 `#8=(#6,#7,#5)`、`#6=(#1,#2)`、`#7=(#3,#4)`:

```
protocol → treasury                         0.0015000
royalty(#8, 0.0135):
  #8 留下 0.0135·0.7                         0.0094500
  分出 0.0135·0.3 = 0.00405,按权重:
    royalty(#6, 0.0013365):
      #6 留下 ·0.7                           0.0009356
      → #1 (叶子)                            0.0001604
      → #2 (叶子)                            0.0002406
    royalty(#7, 0.0013365):
      #7 留下 ·0.7                           0.0009356
      → #3 (叶子)                            0.0002005
      → #4 (叶子)                            0.0002005
    royalty(#5, 0.001377)  (叶子)            0.0013770
                                            ──────────
  子树合计                                    0.0135000   ✓ (= N)
合计  = 0.0015 + 0.0135 = 0.015 = V                       ✓
```

### 5.4 其他经济修正

- **真正把链移出热路径。** 经核验的研究([`research/onchain-micropayments/report.md`](research/onchain-micropayments/report.md))确认:亚分钱、多收款人的 DAG 版税"每次调用即时上链结算"在*所有*链(含 Solana)上都不划算(费用底线 ~$0.0008–$0.0011 = 一次 $0.0003 调用的 2.5–3.7 倍;Solana 逐收款人账户租金 ~$0.30 ≈ 1000 倍;且没有任何链能"微秒级"最终性——最快 ~100–250ms 预确认、Solana ~12.8s 终局)。所以把**链下累计 + 周期性链上批量结算设为 Phase-0 默认**,而非"Phase 2":每次调用用 **Ed25519/EIP-712 签名凭证 + DAG 净额**累计;达**阈值(≥ ~$0.05–$1)或定时**时,通过**内部账本合约**(`balances[addr] += 净额`,无逐人租金 → 一笔可付几百个收款人)**批量上链结算**。结算放 **Base**(复用现有 EVM 合约 + 原生 USDC + **x402**,主流 agent 支付轨,~95% 在 Base);**Hedera HCS-26 继续当注册表**。ρ-flow 决定每人*应得多少*,批量结算决定*何时支付*。
- **对 OPEN 版税要诚实。** 版税只有当调用经过网关时才产生。任何人都能复制 OPEN 技能的明文自托管,花 0 元——*这正是规避版税的方式*。所以 OPEN 收入是**自愿/便利驱动**的,不是强制的。为此设计:让托管路径*更好*(托管更新、缓存、捆绑发现、经认证的质量),让组合者因价值而留下,而非因被迫。把*可强制*的费用留给实现被真正隐藏的技能。
- **按调用版税金额微不足道**(v1 例子:`$0.00027/次`、`$2.70/月`)。价值主张要以发现、质量认证、以及有真实按次/订阅定价的技能为主——而不是长尾被动收入。
- **超额支付:** 按 `pricePerCall` 计算份额并退还多余(或明确说明超额作为打赏)。目前是把整个 `msg.value` 都分掉,不退款。

---

## 6. 去中心化与信任立场

在每份对外文档的开头明确写清:

> **今日信任模型:** SkillNet 是一个中心化的参考服务,加上一份真实的、公开的 Hedera 测试网 HCS-26 注册表。技能的*注册与版本历史*是去中心化且防篡改的;所有权/定价/结算正在迁移到一条测试网上链;执行、发现、质量打分由运营方运行,*尚不*无信任。设计中描述的验证者网络、TEE 执行、链上治理**均未实现。**

HCS-26 真正带来的(及其边界):一份永久的、第三方可验证的、带时间戳的注册日志,运营方无法悄悄改写或下架。**修复:**在每条消息里加入 `contentHash`,使它锚定*技能本身*,而不只是名字。它仍然**不能**证明链下执行与注册的技能一致——别声称它能。

---

## 7. 保留 / 修复 / 砍掉

**保留**
- HCS-26 发布 + mirror node 读取——真正的切入点(加 `contentHash`)。
- 三个 Solidity 合约——干净的基础;把它们部署。
- MCP server + agent demo——证明机器可用(从方向 C 嫁接)。
- 组合-DAG + 版税*概念*——长期差异化。
- 诚实的代码内注释与忠实的 demo。

**修复**
- 版税数学 → 守恒的 ρ-flow(§5.2);加上断言"菱形与束的束情况下 `Σ balances == V`"的不变量/模糊测试。
- 给 `CompositionDAG._isAncestor` 加 visited-set,超过队列上限时**回滚**(而非悄悄丢弃);与 JS 的 visited-set 对齐,使两者行为一致。
- mint 前加上访问控制 / 一个真实(哪怕是链下的)预发布检查,或者干脆去掉"门槛"声明。
- 给 NFT 结构体加 `manifestHash` + `holTopicId`(+ scores 或 `scoresHash`),否则别再声称链上质量。
- 费用数学只留一个事实来源(部署并调用,或对模拟器做一致性测试);用整数/bigint 货币运算,不要浮点。
- 把 v1 §13 改写为"已编写 + 已单测(本地),未部署"。
- **凭证不入库** —— `.env`/`.env.hedera` 不被追踪,仓库里只提交 `.example` 模板。

**砍掉(明确推迟、由需求触发)**
- 质押验证者网络 + 罚没 → 换成一个确定性的、任何人都能复现的检查器(schema lint + 可调用性/成功率测试套件),外加来自真实调用遥测的**基于使用**的声誉。
- TEE 执行与 SHIELDED/PRIVATE 层级 → 先**只上 OPEN**,直到有人真的需要更多;"SHIELDED"现实版 = "运营方持有秘密,只暴露 schema",且只适用于隐藏代码确实能保护价值的技能类型(LoRA 权重、数据集),*而非*提示词。
- 策展市场 / 联合曲线;作为独立产品的状态通道;治理**代币 + DAO**(那几个参数用 3-of-5 多签 + 时间锁治理;任何代币之前先咨询证券法律顾问);跨链;ERC-6551/4337。

---

## 8. 分阶段建设计划

**Phase 0 — 诚实的 MVP(约 2–3 周)。** *把真正的切入点上线。*
- 密钥管理规范:env 文件不入库,只留 `.example` 模板。(第 1 天)
- 带 `contentHash` 的 HCS-26 注册表;一个干净的发现/搜索 UI + MCP `list/search/get`,直接在*实时 mirror node* 上(而非内存状态)。
- 把 v1 现状章节改写到与现实一致;加上"今日信任模型"框。
- *交付物:* 一个可公开验证、不可下架的技能注册表,带 agent 可用的发现。零虚假声明。

**Phase 1 — 在一条测试网上做实经济(约 3–5 周)。**
- 在 `FeeRouter.sol` 里实现 ρ-flow 版税;加不变量 + 模糊测试(菱形、束的束、不均权重、深度截断、守恒)。
- 给 `SkillNFT` 加 `manifestHash`/`holTopicId`/`scoresHash`;给 `CompositionDAG` 加 visited-set。
- 把三者部署到**一条**链(Base Sepolia *或* Hedera EVM——二选一),验证,填地址,让 demo 经 viem 调用它们。在不同 DAG 形状下测量真实 `payForCall` gas。
- 链下凭证计费 + 单一批量结算路径。**已出原型(2026-06-22):** `SettlementVault.sol`(EIP-712 累计支出凭证、内部账本批量结算、一笔交易给 100 个收款人记账、链上强制守恒)+ 链下累计引擎 `skill-network/settlement/engine.mjs`;10 个 Foundry 测试通过 + 一个 demo 把 **200 次亚分钱调用压缩成 2 笔交易(100×)**,精确守恒。见 `skill-network/settlement/README.md`。
- *交付物:* mint → compose → call → **守恒**版税,真正上链,且 gas 已实测。

**Phase 2 — 质量与 agent 闭环(由需求触发)。**
- 确定性质量检查器 + 基于使用的声誉;由它把 `scoresHash` 写上链。
- 把 MCP/agent 路径硬化成可信的运行时闭环(从方向 C 嫁接);只有到那时才重新考虑*真实*的 x402 集成。

**Phase 3+ — 仅当被真实量拉动时。** 质押验证者、TEE/SHIELDED、策展、作为产品的状态通道、治理代币。

---

## 9. Demo 计划

把"带捏造验证者共识的市场" demo 换成一个**可信、诚实**的:

1. **注册**一个技能 → 展示真实的 HCS-26 交易 id + mirror node 链接(任何人可验证、你无法伪造)。
2. **发现**——从一个真实 agent(Claude / Qwen agent)经 MCP server——"帮我找一个能审计 Solidity 的技能",在实时注册表上解析。
3. **组合 + 调用**——在已部署测试网上 → 展示**守恒**的费用分账,带一次实时余额检查,其和等于支付额(正是目前坏掉的那个点)。
4. 把每个模拟元素都标注为模拟。可信度*本身*就是 demo。

---

## 10. 风险与缓解

| 风险 | 缓解 |
|---|---|
| 凭证管理 | 密钥只存在于不被追踪的 `.env` 文件里(仓库只含 `.example` 模板);托管部署使用密钥管理器。 |
| 版税修复本身就是错的 | 归纳证明(§5.2)+ 在随机 DAG 上断言 `Σ balances == V` 的不变量/模糊测试,先于任何声明。 |
| 重新引入不诚实声明 | 每份对外文档都带"今日信任模型"框;现状表写明 已建/部分/模拟。 |
| 没有用户 / 投机性需求 | Phase 0 卖唯一有真实价值的东西(可验证注册表);在做经济之前先用真实创作者验证。 |
| 对 HOL/Hashgraph 的平台依赖 | HCS-26 是一个我们直接控制的、薄而标准的 HCS-topic 模式;上线 Phase 0 不依赖 Registry Broker 或 HOL 更上层。 |
| 链上结算比支付本身还贵 | Phase 1 实测 gas;默认链下凭证 + 批量结算。 |
| OPEN 复制自托管击穿版税 | 不要营销 OPEN 被动收入;以托管路径的价值竞争;只在代码被隐藏处强制收费。 |

---

## 11. 留给负责人的开放决策

1. **主方向:** 确认 **A 为主干**(推荐),还是选 B(全链上)或 C(agent 经济)作为主押注?*会重塑计划。*
2. **链:** Phase 1 用 Base Sepolia 还是 Hedera EVM。(二选一。Hedera 让一切与 HCS-26 在同一生态;Base 的 EVM 工具更成熟。)
3. **层级:** 现在就**只上 OPEN**(推荐),还是保留 3 层标签作为前瞻性元数据但不带 TEE 支撑?
4. **质量信号:** 确定性检查器 + 使用声誉(推荐) vs 以后投入做真实验证者网络。
5. **链上分数:** 存 5 个分数,还是只存一个 `scoresHash`?

### 立即可做的下一步(与方向选择无关)
1. **密钥管理规范:`.env`/`.env.hedera` 保持不入库;托管部署走密钥管理器。**(今天)
2. 把 v1 §13 与安全/经济表格改写到与现实一致;加上"今日信任模型"框。
3. 落地 **ρ-flow 版税修复** + 一个不变量测试:它在今天的 `FeeRouter` 上失败、修复后通过——这是单点价值最高的改动,能为整个经济故事去风险。
4. 给 HCS-26 消息加 `contentHash`,让注册表锚定完整性。

---

*附:来源——本重新设计基于一次 4 维审计(合约正确性与版税数学、去中心化现实、经济与激励、范围/市场),共 40 条带文件级证据的发现。守恒版税公式及其证明见 §5.2。*
